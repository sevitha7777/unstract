apiVersion: apps/v1
kind: Deployment
metadata:
  name: unstract-api-deployment-workers
  namespace: medical-claims
spec:
  replicas: 1
  selector:
    matchLabels:
      app: unstract-api-deployment-workers
  template:
    metadata:
      labels:
        app: unstract-api-deployment-workers
    spec:
      serviceAccountName: medical-claims-sa
      volumes:
      - name: unstract-files
        persistentVolumeClaim:
          claimName: unstract-efs-pvc
      containers:
      - name: api-deployment-workers
        image: 576245601309.dkr.ecr.us-east-1.amazonaws.com/unstract/workers:v1.41
        command: ["/app/run-worker.sh", "api-deployment"]
        securityContext:
          runAsUser: 0
          runAsGroup: 0
        envFrom:
        - configMapRef:
            name: unstract-config
        - secretRef:
            name: unstract-secrets
        env:
        - name: DJANGO_APP_BACKEND_URL
          value: "http://unstract-backend:8000"
        - name: INTERNAL_API_BASE_URL
          value: "http://unstract-backend:8000/internal"
        - name: CELERY_BROKER_CONNECTION_RETRY_ON_STARTUP
          value: "true"
        - name: CELERY_BROKER_CONNECTION_RETRY
          value: "true"
        - name: CELERY_BROKER_CONNECTION_MAX_RETRIES
          value: "10"
        - name: CELERY_BROKER_HEARTBEAT
          value: "60"
        - name: CELERY_WORKER_CANCEL_LONG_RUNNING_TASKS_ON_CONNECTION_LOSS
          value: "true"
        - name: CELERY_TASK_REJECT_ON_WORKER_LOST
          value: "true"
        - name: CELERY_QUEUES
          value: "celery_api_deployments"
        volumeMounts:
        - name: unstract-files
          mountPath: /tmp/prompt-studio-data
          subPath: prompt-studio-data
        - name: unstract-files
          mountPath: /tmp/workflow-execution
          subPath: workflow-execution
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "500m"